name: Build and Deploy to AKS

on: workflow_dispatch

env:
  REGISTRY: ${{ secrets.ACR_LOGIN_SERVER }}
  IMAGE_NAME: todo-api

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read

    steps:
      - uses: actions/checkout@v3

      - name: Azure Login with OIDC
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Build and push Docker image with ACR
        run: |
          az acr build \
            --registry ${{ secrets.ACR_NAME }} \
            --image ${{ env.IMAGE_NAME }}:${{ github.sha }} \
            --image ${{ env.IMAGE_NAME }}:latest \
            .

      - name: Update deployment image
        run: |
          sed -i 's|<ACR_LOGIN_SERVER>|${{ env.REGISTRY }}|g' k8s/deployment.yml
          sed -i 's|<IMAGE_TAG>|${{ github.sha }}|g' k8s/deployment.yml

      - name: Attach ACR to AKS
        run: |
          az aks update \
            --resource-group ${{ secrets.AKS_RESOURCE_GROUP }} \
            --name ${{ secrets.AKS_CLUSTER_NAME }} \
            --attach-acr ${{ secrets.ACR_NAME }}

      - name: Set AKS context
        run: |
          az aks get-credentials --resource-group ${{ secrets.AKS_RESOURCE_GROUP }} \
            --name ${{ secrets.AKS_CLUSTER_NAME }} --overwrite-existing

      - name: Create prerequisites
        run: |
          kubectl create serviceaccount todo-api --dry-run=client -o yaml | kubectl apply -f -
          kubectl create secret generic todo-api-secrets \
            --from-literal=db-server=${{ secrets.DB_SERVER }} \
            --from-literal=db-name=${{ secrets.DB_NAME }} \
            --dry-run=client -o yaml | kubectl apply -f -

      - name: Deploy to AKS
        run: |
          kubectl apply -f k8s/deployment.yml
          kubectl apply -f k8s/service.yml
          kubectl rollout status deployment/todo-api -n default --timeout=300s

      - name: Verify deployment
        run: |
          kubectl get pods
          kubectl get svc
